---
- name: Install Open Game Backend Auth
  hosts: auth
  remote_user: "{{ system_user_auth }}"
  vars_files:
    - group_vars/vars
    - group_vars/vault
  roles:
    - role: deploy_spring_boot_service
      vars:
        service_name: auth
